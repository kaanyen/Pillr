import 'package:flutter/material.dart';

import '../../../common/widgets/feature_placeholder_scaffold.dart';

class PartnersListScreen extends StatelessWidget {
  const PartnersListScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const FeaturePlaceholderScaffold(
      title: 'Partners',
      message: 'Searchable directory and profiles for pastors land in Phase 2.',
    );
  }
}
