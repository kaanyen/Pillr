{
  "name": "pillr-functions",
  "private": true,
  "engines": {
    "node": "20"
  },
  "main": "lib/index.js",
  "scripts": {
    "build": "tsc",
    "serve": "npm run build && firebase emulators:start --only functions",
    "lint": "eslint --ext .ts src/",
    "seed-demo": "node scripts/seed-demo.cjs"
  },
  "dependencies": {
    "@types/pdfkit": "^0.17.5",
    "firebase-admin": "^12.6.0",
    "firebase-functions": "^6.0.1",
    "pdfkit": "^0.18.0",
    "resend": "^4.0.0"
  },
  "devDependencies": {
    "typescript": "^5.6.3"
  }
}
